# Attic

Reference donor code lives here.

Nothing in this folder should be compiled directly into the app unless it is
first rewritten or extracted into `src/` and `include/`.

The first donor source is ictgHax timing/MIDI code.
